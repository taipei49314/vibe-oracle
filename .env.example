# SpaceXAI / xAI (OpenAI-compatible) — server only, never expose to the browser
XAI_API_KEY=
XAI_BASE_URL=https://api.x.ai/v1
XAI_MODEL=grok-4.5

# Server
PORT=8787
# When no API key: still returns a full theatrical report (demo mode)
ALLOW_DEMO_WITHOUT_KEY=true
