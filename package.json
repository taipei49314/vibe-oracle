{
  "name": "vibe-oracle",
  "version": "0.1.0",
  "private": true,
  "description": "VibeOracle - pure vibe life reports. Not evidence. Not your usual stack.",
  "scripts": {
    "dev": "concurrently -n server,client -c magenta,cyan \"npm run dev -w server\" \"npm run dev -w client\"",
    "dev:server": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client && npm run build -w server",
    "test": "npm run test -w server",
    "smoke": "node scripts/smoke.mjs",
    "demo:api": "node scripts/demo-api.mjs",
    "eval:prompts": "node eval/run-prompt-assertions.mjs",
    "check": "npm run test && npm run eval:prompts && npm run build",
    "start": "npm run start -w server"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "esbuild@0.25.12": true
  }
}
